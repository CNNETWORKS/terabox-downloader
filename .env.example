# Copy this file to .env and fill the real values. DO NOT commit your .env
BOT_TOKEN=""
API_ID=""
API_HASH=""
# Comma separated admin ids (example: 12345678,87654321)
ADMIN_ID=""
# Optional: initial force-sub channel (username or id)
FORCE_SUB_CHANNEL=""
# Optional: dumb channel (username or id)
DUMB_CHANNEL=""
# API template: default uses your replacement API
API_TEMPLATE="https://teradl.tiiny.io/?key=RushVx&link={link}"
# TMP spooled memory limit in MB (for spooled temp file approach)
TMP_SPOOL_LIMIT_MB="16"
# Optional: maximum allowed file size in bytes (default: 0 = no limit)
MAX_FILE_SIZE="0"
# Optional temporary directory for disk mode (leave blank to use system temp)
TMP_DIR=""
# Optional: user session string (if you want to use a userbot session for >2GB uploads)
USER_SESSION_STRING=""
